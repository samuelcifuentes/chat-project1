Coloca aquí el archivo Chat.js generado con:
slice2js --output-dir public/ice ../src/main/slice/chat.ice

El archivo debe exponer el módulo `Chat` para que el bundle pueda acceder a las proxies de Ice.

